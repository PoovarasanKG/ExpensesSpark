<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensesspark">

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.AccountSetings"></activity>
        <activity android:name=".activity.CommonDetailsActivity" />
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:screenOrientation="portrait"
                    android:theme="@style/NoActionBarAppTheme" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name1"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activity.AddTransaction"
            android:label="Add Transaction"
            android:parentActivityName=".activity.NewAccountActivity" />
        <activity
            android:name=".activity.NewAccountActivity"
            android:label="@string/app_name2"
            android:parentActivityName=".activity.AddTransaction" />
        <activity
            android:name=".activity.TransactionsListActivity"
            android:label="Transaction List"
            android:parentActivityName=".activity.AddTransaction" />
        <activity
            android:name=".activity.AccountListActivity"
            android:label="Account List"
            android:parentActivityName=".activity.AccountListActivity" />
        <activity
            android:name=".activity.Dashboard"
            android:label="Dashboard"
            android:parentActivityName=".activity.SplashScreen" />
    </application>

</manifest>